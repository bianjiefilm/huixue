# 学生学业表现基础统计 (校情数据分析 第 1 关)

## 第一章 校情数据分析背景

### 1.1 校情数据的来源

高校校情数据来自三类系统:

- **教务系统**: 学籍, 课表, 选课, 成绩
- **学习平台**: 登录记录, 资源访问, 提交流水
- **行政系统**: 学院/专业归属, 教师档案, 学期日历

不同系统数据格式各异 (CSV / JSON / SQL 视图), 但落地分析时常归并成两类结构: 行式记录 (`list[dict]`) 与时序数值 (`list[float]`)。本关聚焦前者。

### 1.2 校情分析的 4 维度

| 维度 | 关注 | 决策面向 |
|---|---|---|
| 学业成绩 | 通过率 / 分数分布 / 不及格率 | 教务老师, 学院 |
| 命题质量 | 题目难度 / 区分度 | 命题教师 |
| 选课行为 | 集中度 / 热门冷门 | 教务管理 |
| 学生群体 | GPA 分布 / 能力分层 | 学院, 辅导员 |

本关 4 个函数对应这 4 个维度, 每个函数算一个核心指标。

### 1.3 数据治理与隐私

校情数据涉及学生信息, 必须遵守:

- 学生 ID 去标识化 (`S001` / `hash` 形式, 不带身份证号 / 真名)
- 群体统计为主 (不分析单个学生轨迹)
- 教师评价匿名 (聚合到课程级别再公开)

本关训练数据均为合成数据, 无真实学生信息。

### 1.4 本关 4 函数的定位

| 函数 | 维度 | 决策含义 |
|---|---|---|
| `compute_pass_rate` | 学业成绩 | 教学质量底线 |
| `item_difficulty` | 命题质量 | 命题难度合理性 |
| `enrollment_concentration` | 选课行为 | 选课系统设计反馈 |
| `gpa_distribution_buckets` | 学生群体 | 整体水平形态 |

---

## 第二章 4 函数概念与计算

### 2.1 通过率 (`compute_pass_rate`)

最简单的教学质量指标:

$$\text{pass\_rate} = \frac{\text{pass\_count}}{\text{total\_count}}$$

其中 `pass_count = sum(1 for g if g["score"] >= threshold)`。

**业务实务**:

| 通过率 | 判定 | 行动 |
|---|---|---|
| > 95% | 命题过简 | 提高难度 |
| 75% - 85% | 健康区间 | 维持 |
| 60% - 75% | 偏低 | 复盘教学 |
| < 60% | 严重偏低 | 全面调整 |

**返回结构**: 函数返回 `dict` 含 3 个 key — `pass_count` / `fail_count` / `pass_rate`。这种"计数 + 比率"组合是统计指标的常见返回形式, 方便上层直接展示。

### 2.2 题目难度系数 (`item_difficulty`)

教育测量学经典指标 (Anastasi & Urbina, *Psychological Testing*, 1997):

$$P = \frac{\bar{x}}{x_{\max}}, \quad P \in [0, 1]$$

| P | 判定 |
|---|---|
| > 0.7 | 简单 |
| 0.4 ≤ P ≤ 0.7 | 适中 |
| < 0.4 | 困难 |

**业务实务**: 期末考试 P 应在 0.55-0.65 区间。命题专家常用此指标筛选题库, 偏离区间的题目要重新审核或剔除。

注意: P 是单道题的难度, 不是整张试卷的难度。一份合理的试卷应该混合不同难度题目。

### 2.3 选课集中度 (`enrollment_concentration`)

赫芬达尔-赫希曼指数 (Herfindahl-Hirschman Index, HHI), 经济学市场集中度概念迁移到教育:

$$\text{HHI} = \sum_i s_i^2, \quad s_i = \frac{\text{enroll}_i}{\sum_j \text{enroll}_j}$$

每门课的选课份额平方和。份额越集中, HHI 越接近 1; 越分散, 越接近 0。

| HHI | 判定 | 含义 |
|---|---|---|
| < 0.2 | 分散 | 选课分布健康 |
| 0.2 - 0.5 | 中度集中 | 有几门热门课 |
| > 0.5 | 严重集中 | 选课系统设计需调整 |

**业务实务**: HHI 高时常见原因是某些课程容量小但需求大 (如热门导师), 或选课规则诱导扎堆 (高分课). 可加大热门课容量或拆分。

### 2.4 GPA 分布桶 (`gpa_distribution_buckets`)

按预定区间分桶统计:

- 输入 GPA 列表与分桶边界 (例如 `[0, 1.0, 2.0, 3.0, 4.0]`)
- 输出每段人数 dict, key 形如 `"0.00-1.00"`

**分桶规则**:

- 段 `[b_i, b_{i+1})` 左闭右开
- 最后段 `[b_{n-1}, b_n]` 闭区间 (确保最大值落进最后一桶)

**业务实务**:

- **正态偏右**: 健康, 大多数学生在 2.5-3.5
- **双峰**: 教学分化, 一部分学生跟得上, 一部分掉队
- **长尾低值**: 学院需补救教学

---

## 第三章 业务案例 + 工程口诀

### 3.1 案例: 某高校 Python 课 100 学生

| 指标 | 数据 | 计算 | 结果 |
|---|---|---|---|
| 通过率 (F1) | 75 通过 / 25 不及格 | 75 / 100 | 0.75 (健康) |
| 难度 (F2) | 平均 68 / 100 | 68 / 100 | 0.68 (适中) |
| 选课集中度 (F3) | 5 方向 [40,30,15,10,5] | 0.16+0.09+0.0225+0.01+0.0025 | **0.285** (中度) |
| GPA 分布 (F4) | buckets=[0,1,2,3,4] | [5, 15, 35, 30, 15] | 正态偏右 |

四指标横向看, 这门课整体健康, 主要问题在选课集中度偏高, 反映课程容量配置可优化。

### 3.2 工程口诀

- **75-85%** 通过率是健康区间
- 难度 P 应该 **0.5-0.7**
- HHI **< 0.3** 选课分散健康
- GPA **双峰是警讯**, 单峰正态是健康

---

## 第四章 现代趋势

学习分析 (Learning Analytics) 与教育数据挖掘 (Educational Data Mining, EDM) 是 21 世纪起步的领域。本关用基础描述性统计指标, 进阶可参考:

- **项目反应理论** (IRT): 同时估计学生能力 + 题目难度 + 区分度三个参数, 比单一难度系数更精确
- **学习路径预测**: 序列模型预测学生何时退课或挂科
- **自适应学习系统**: 基于学生历史能力动态推荐下一题

但**本关聚焦基础**: 把这 4 个底层指标算扎实, 是后续高级分析的前提。

### 4.1 实施提示

实现时注意:

- 输入校验 (空列表 / 字段缺失 / 类型错) 必须 raise, 不能默默返回 0
- 浮点数对比用 `abs(x - expected) < tolerance`
- 字典返回必须含完整 key, 不能省略
- 函数应该 **pure** (无副作用, 同输入同输出)

这是数据分析工程师的基本素养。
