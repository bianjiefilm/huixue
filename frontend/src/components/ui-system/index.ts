/**
 * UI System Components
 *
 * Custom UI components for the education platform.
 * Note: Naive UI wrappers have been removed. Using Ant Design Vue instead.
 */

// Custom components (no external UI library dependency)
export { default as DarkCard } from './DarkCard.vue'
export { default as ProgressBar } from './ProgressBar.vue'
export { default as PriorityBadge } from './PriorityBadge.vue'
export { default as GlowButton } from './GlowButton.vue'
export { default as SearchInput } from './SearchInput.vue'

// Note: Naive UI wrapper components (Button, Input, Modal, etc.) have been removed.
// Please use Ant Design Vue components directly instead:
// - a-button, a-input, a-modal, a-dropdown, a-tooltip, a-avatar, a-badge, etc.
